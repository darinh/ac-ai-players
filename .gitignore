# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Build artifacts
bin/
obj/
*.user

# Python (for any future tooling)
__pycache__/
*.pyc
.venv/
venv/

# Node (for any future tooling)
node_modules/

# Local secrets / model configs
.env
.env.local
*.local.json

# Local-only: agent worktrees (see .github/skills/create-new-branch)
.worktrees/

# Local-only: Copilot CLI session state (per-machine agent scratch)
.copilot/

# Local-only: headless-client telemetry sinks (LLM decisions + nav graph)
# Source lives in the worktrees.
experiments/headless-client/data/

# Local-only: regeneratable output from WorldNavBuilder. Reference
# outputs live in experiments/world-nav/reference/ instead.
experiments/world-nav/data/

# Local-only: agent worktrees (see .github/skills/create-new-branch)
.worktrees/

# Local-only: stray review/diff artifacts and live-run logs
*.diff
session-state-logs/
